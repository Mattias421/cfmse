#!/usr/bin/env bash
# Run Python from this checkout's uv-managed environment.
set -Eeuo pipefail

repo_root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
cd "${repo_root}"
exec "${CFMSE_UV:-uv}" run --no-project python "$@"
