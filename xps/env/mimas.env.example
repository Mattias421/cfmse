# Optional Mimas overrides. Normally no file is needed: runtime.sh uses
# $EXP/cfmse, $DATA/VB+DMD, $EXP/cfmse/logs, and Python through uv run.
# Required only for the weights-only fine-tune:
# export CFMSE_ICFM_FULL_CKPT=/path/to/data_icfm_full/checkpoints/best-pesq-XX.ckpt
# export WANDB_MODE=offline
# export CFMSE_NOLOG=1
